export type NewProfile = {
  displayName: string;
  profilePicture: string;
  email: string;
  username: string;
  emailVerified: boolean;
};

export type ProfileEditObj = {
  displayName: string;
  profilePicture: string;
  email: string;
  emailVerified: boolean;
};
